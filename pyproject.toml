[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "personal-knowledge-base"
version = "1.0.0"
description = "Personal knowledge base with RAG, Web UI, MCP, and a global CLI"
requires-python = ">=3.10"
dynamic = ["dependencies"]

[project.scripts]
knowledge = "src.cli.entry:main"

[tool.setuptools]
py-modules = ["config"]

[tool.setuptools.packages.find]
include = ["src*"]
exclude = ["tests*"]

[tool.setuptools.dynamic]
dependencies = {file = ["requirements.txt"]}

[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = ["data", "chroma_db", "web/node_modules", "web/dist", ".git"]
